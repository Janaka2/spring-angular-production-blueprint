/**
 * The domain: aggregates, value objects and the rules that must hold whatever the transport or the database.
 * Depends only on the JDK, jakarta.persistence and jakarta.validation (ADR-011). Enforced by ArchitectureTest.
 */
@org.jspecify.annotations.NullMarked
package me.janaka.assetcare.domain;
